version: "3.9"

services:
  web:
    build: .
    container_name: hormozgan-driver-web
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    command: npm startversion: '3.8'

services:
  postgres:
    image: postgis/postgis:15-3.3
    environment:
      POSTGRES_DB: hormozgan_driver
      POSTGRES_USER: developer
      POSTGRES_PASSWORD: dev123456
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7.2-alpine
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes

volumes:
  postgres_data:
